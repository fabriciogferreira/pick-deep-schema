services:
  pick-deep-schema:
    user: 1000:1000
    image: oven/bun:1.3.9-slim
    command: [ "bun install && tail -f /dev/null" ]
    volumes:
      - ./:/srv/
    entrypoint: [ "sh", "-c" ]
    working_dir: /srv
    container_name: pick-deep-schema
